"""Fuzzy-match entity-resolution reference pipeline (Databricks-native, batch).

Bounded units connected by Delta-table contracts. See ``sql/contracts.sql`` for
the seam tables and each ``pipeline/<unit>.py`` for a unit's logic.
"""
