bundle:
  name: watchdog-guardrails

targets:
  dev:
    mode: development

  prod:
    mode: production

resources:
  apps:
    watchdog_guardrails:
      name: "watchdog-guardrails"
      description: "AI Guardrails MCP — safe data access enforcement"
      source_code_path: .
