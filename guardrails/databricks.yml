bundle:
  name: watchdog-guardrails

targets:
  fe-stable:
    mode: production
    workspace:
      profile: fe-stable

resources:
  apps:
    watchdog_guardrails:
      name: "watchdog-guardrails-fe-stable"
      description: "AI Guardrails MCP — safe data access enforcement"
      source_code_path: .
